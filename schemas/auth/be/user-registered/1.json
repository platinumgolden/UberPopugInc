{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Auth.UserRegistered.v1",
  "description": "json schema for auth user registered",
  "type": "object",
  "definitions": {
    "eventData": {
      "type": "object",
      "properties": {
        "publicId": {
          "type": "string",
          "format": "uuid",
          "description": "This is publicId of user"
        },
        "role": {
          "type": "string"
        },
        "email": {
          "type": "string",
          "format": "email"
        }
      },
      "required": ["publicId", "role", "email"]
    }
  },
  "properties": {
    "eventId": {
      "type": "string",
      "format": "uuid"
    },
    "eventVersion": {
      "type": "number",
      "enum": [1]
    },
    "eventTime": {
      "type": "string",
      "pattern": "^[0-9]+$"
    },
    "eventName": {
      "type": "string"
    },
    "producer": {
      "type": "string"
    },

    "data": {
      "$ref": "#/definitions/eventData"
    }
  },
  "required": [
    "eventId",
    "eventVersion",
    "eventTime",
    "eventName",
    "producer",
    "data"
  ]
}
