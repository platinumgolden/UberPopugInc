apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: popugjira
spec:
  rules:
    - http:
        paths:
          - path: /auth/*
            pathType: ImplementationSpecific
            backend:
              service:
                name: auth
                port:
                  number: 3003
